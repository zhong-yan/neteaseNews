from .spider.config import *

timeout = 1
# for i in air.distinct('articleUrl'):
#     timeout += 1
#     print('第{0}文章URL:{1}'.format(timeout, i))
print('\t{}表里面一共文档数:{}'.format(MONGODB_TABLE_0, world.find().count()))
